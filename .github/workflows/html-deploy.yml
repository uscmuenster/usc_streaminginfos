name: Deploy Pages (manual changes)

on:
  push:
    branches: [ main ]
    paths:
      - 'docs/**'

permissions:
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
